{
  "name": "minecraft-2d",
  "version": "1.0.0",
  "description": "2D Minecraft browser game",
  "scripts": {
    "build": "esbuild game.js --bundle --outfile=bundle.js --format=iife --platform=browser",
    "test": "playwright test",
    "test:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0",
    "esbuild": "^0.20.0",
    "serve": "^14.2.0"
  }
}
